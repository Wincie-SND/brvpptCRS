class Booking:

    def __init__(
        self,
        id,
        renter_id,
        vehicle_id,
        status
    ):

        self.id=id
        self.renter_id=renter_id
        self.vehicle_id=vehicle_id
        self.status=status